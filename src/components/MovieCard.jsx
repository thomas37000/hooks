import React from 'react';
import { Button, Card, CardDeck} from  'react-bootstrap';
// import './QuoteCard.css';

export default function MovieCard() {
  return(
    <>
      {/* <CardDeck>
      <Card>
        

      <Card.Img variant="top" src={movieList.img} alt={name} />
      <Card.Body>
      <Card.Title><h2>{name}</h2></Card.Title>
      <Card.Text><h3>date: {date}</h3></Card.Text>
      </Card.Body> 
      </Card>  
      </CardDeck>  */}
    </>
  )
}
// const MovieCard = ({ name, image, date }) => (
//   <figure className="MovieCard">
//     <img src={image} alt={name} />
//     <figcaption>
//       <blockquote>{date}</blockquote>
//     </figcaption>
//   </figure>
// );

{/* {
          movies.map(movie => (
            <li key={movie.id}>
                {movie.name}
                <img className="avatar" src={movie.img} alt={movie.name} />
            </li>))
        } */}